const Layout = (props) => {
    return (
        <>
        {/* contoh cara pembuatan props children di react */}
            {props.children}
        </>
    )
}
export default Layout;
//untuk mengetahui hasil dari penggunaan props children lihat di  file App.js